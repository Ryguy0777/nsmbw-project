#pragma once

namespace std
{

template <class T>
constexpr const T& min(const T& l, const T& r)
{
    return (r < l) ? r : l;
}

template <class T>
constexpr const T& max(const T& l, const T& r)
{
    return (l < r) ? r : l;
}

} // namespace std